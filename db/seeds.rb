# This file should contain all the record creation needed to seed the database with its default values.
# The data can then be loaded with the rails db:seed command (or created alongside the database with db:setup).
#
# Examples:
#
#   movies = Movie.create([{ name: 'Star Wars' }, { name: 'Lord of the Rings' }])
#   Character.create(name: 'Luke', movie: movies.first)



demo = User.new({username: "docdoom815", email: "docdoom815@gmail.com", password: "funfunfun1"})
demo.save!


    # track_one = Track.new({
    #     title: "crazy beats",
    #     user_id: User.first.id,
    # })
    # track_one.track.attach(io: File.open('‎⁨/Ronald/Users⁩/Ronald_Crecco⁩/Desktop⁩/TONEFEEDTRACKS/test1.mp3'), filename: "test1.mp3")
    # track_one.save!
    # track_two = Track.new({
    #     title: "techno song!",
    #     user_id: User.first.id,
    # })
    # track_two.save!
    # track_three = Track.new({
    #     title: "TRAP MUSIC",
    #     user_id: User.first.id,
    # })
    # track_three.save!
    # track_four = Track.new({
    #     title: "EDM Vol 1",
    #     user_id: User.first.id,
    # })
    # track_four.save!
    # track_five = Track.new({
    #     title: "EXPERIMANTAL SONG",
    #     user_id: User.first.id,
    # })
    # track_five.save!
    # track_six = Track.new({
    #     title: "CINEMATIC 1",
    #     user_id: User.first.id,
    # })
    # track_six.save!
    # track_seven = Track.new({
    #     title: "Cinematic 2",
    #     user_id: User.first.id,
    # })
    # track_seven.save!
    # track_eight = Track.new({
    #     title: "PERCUSSION!",
    #     user_id: User.first.id,
    # })
    # track_eight.save!
